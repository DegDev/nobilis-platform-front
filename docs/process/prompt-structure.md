# Prompt Structure — pointer (SSOT in the back canon)

**The prompt-structure canon is single and lives in the back repo:**
`nobilis-platform-back/docs/process/prompt-structure.md`
([relative link](../../../nobilis-platform-back/docs/process/prompt-structure.md), resolves
when the two repos are checked out as siblings). It is project-agnostic and shared by BOTH repos —
the portable layer (prompt format, the type taxonomy, the pre-send checklist, the `/clear`/`/plan`
flow). Provenance of the rules is in `nobilis-platform-back/docs/sources-log.md`.

This file holds **front-specific lessons only**, if any arise (otherwise it stays a pointer).
**No duplication of the shared rules** (SSOT): a shared rule is added or changed in the back canon,
not copied here — a copy would drift from the canon.

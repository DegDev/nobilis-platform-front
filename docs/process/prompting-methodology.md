# Prompting Methodology — pointer (SSOT in the back canon)

**The prompt-methodology canon is single and lives in the back repo:**
`nobilis-platform-back/docs/process/prompting-methodology.md`
([relative link](../../../nobilis-platform-back/docs/process/prompting-methodology.md), resolves
when the two repos are checked out as siblings). It is project-agnostic and shared by BOTH repos —
the portable layer. Provenance of the rules is in `nobilis-platform-back/docs/sources-log.md`.

This file holds **front-specific lessons only**, if any arise (otherwise it stays a pointer).
**No duplication of the shared rules** (SSOT): a shared rule is added or changed in the back canon,
not copied here — a copy would drift from the canon.

Prompt format and the type taxonomy (RECON / BUILD / RULE / NEW-FEATURE / FIX / architecture-backlog
/ DOCS) live in `nobilis-platform-back/docs/process/prompt-structure.md`.

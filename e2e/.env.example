# Admin login e2e credentials — copy to a local env / .env (gitignored) and fill in.
# These MUST match the admin backend's admin-login configuration. Never commit real values.
NOBILIS_E2E_ADMIN_EMAIL=admin@example.org
NOBILIS_E2E_ADMIN_PASSWORD=replace-with-the-plaintext-password

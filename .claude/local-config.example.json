{
  "_comment": "Copy to local-config.json (gitignored) and set your local absolute path to the backend repo. Used by the /plan command to write the paired backend plan for fullstack features.",
  "backend_path": "/absolute/path/to/nobilis-platform-back"
}

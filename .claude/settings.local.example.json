{
  "_comment": "Copy to settings.local.json (gitignored) for machine-specific permissions. Example: allow CC to read the sibling backend repo for the API contract in paired fullstack plans, or allow MCP tools you use locally (e.g. Figma). Replace the absolute path with your own.",
  "permissions": {
    "allow": [
      "Read(//absolute/path/to/nobilis-platform-back/**)"
    ]
  }
}
